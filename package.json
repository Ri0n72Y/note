{
  "name": "digso.note",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
